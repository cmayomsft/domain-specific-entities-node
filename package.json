{
    "name": "intentalyzer-monorepo",
    "private": true,
    "scripts": {
        "postinstall": "npm run bootstrap",
        "bootstrap": "lerna bootstrap --hoist",
        "build": "tsc -b",
        "lint": "lerna run --parallel lint",
        "test": "lerna run --parallel test"
    },
    "devDependencies": {
        "@types/jest": "^24.0.11",
        "jest": "^24.5.0",
        "lerna": "^3.13.1",
        "ts-jest": "^24.0.0",
        "tslint": "^5.14.0",
        "typescript": "^3.3.4000"
    },
    "dependencies": {}
}
